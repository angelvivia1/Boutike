<!-- Static navbar -->
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="<%= root_path %>">Control Boutique</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li ><a href="<%= user_path %>">Home</a></li>
        <li ><a href="<%= employees_path %>">Empleados</a></li>
        <li ><a href="<%= proveedors_path %>">Proveedores</a></li>
        <li><a href="<%= products_path %>">Productos</a></li>
        <li><a href="#">Ventas</a></li>
        <li ><a href="<%= catalogs_path %>">Galeria</a></li>
        <li class="active" ><a href="<%= catalogo_usuarios_path %>">Catalogo de Usuarios</a></li>
        <li ><a href="<%= us_index_path %>">Nosotros</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li ></li>

        <li><%= link_to "Cerrar sesion", logout_path, method: :delete %></li>
      </ul>
    </div><!--/.nav-collapse -->
  </div><!--/.container-fluid -->
</nav>

<p id="notice"><%= notice %></p>
<div class="jumbotron">
  <h2 class="sub-header">Lista de Usuarios</h2>
  <div class="table-responsive">
    <table class="table table-striped">
      <thead>
      <tr>
      <th>Tipo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @catalogo_usuarios.each do |catalogo_usuario| %>
      <tr>
        <td><%= catalogo_usuario.tipo %></td>
        <td><%= link_to 'Show', catalogo_usuario, :class => 'btn btn-info'  %></td>
        <td><%= link_to 'Edit', edit_catalogo_usuario_path(catalogo_usuario), :class => 'btn btn-warning'  %></td>
        <td><%= link_to 'Destroy', catalogo_usuario, :class => 'btn btn-danger', method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
  <% end %>
  </tbody>
    </table>
  </div>
</div>

<br>

<%= link_to 'Usuario', new_catalogo_usuario_path, :class => "btn btn-success" %>
