

  <!-- Static navbar -->
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand" href="<%= root_path %>">Control Boutique</a>
      </div>
      <div id="navbar" class="navbar-collapse collapse">
        <ul class="nav navbar-nav">
          <li ><a href="<%= user_path %>">Home</a></li>
          <li class="active" ><a href="<%= employees_path %>">Empleados</a></li>
          <li ><a href="<%= proveedors_path %>">Proveedores</a></li>
          <li><a href="<%= products_path %>">Productos</a></li>
          <li><a href="#">Ventas</a></li>
          <li ><a href="<%= catalogs_path %>">Galeria</a></li>
          <li ><a href="<%= catalogo_usuarios_path %>">Catalogo de Usuarios</a></li>
          <li ><a href="<%= us_index_path %>">Nosotros</a></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <li ></li>

          <li><%= link_to "Cerrar sesion", logout_path, method: :delete %></li>
        </ul>
      </div><!--/.nav-collapse -->
    </div><!--/.container-fluid -->
  </nav>




    <p id="notice"><%= notice %></p>
  <div class="jumbotron">
    <h2 class="sub-header">Lista de empleados</h2>
    <div class="table-responsive">
      <table class="table table-striped">
        <thead>
      <tr>
        <th>Nombre(s)</th>
        <th>Apellido(s)</th>
        <th>Ciudad</th>
        <th>Colonia</th>
        <th>Calle y Numero</th>
        <th>Email:</th>
        <th>Edad</th>
        <th>Celular</th>
        <th>Fecha de Contratación</th>
        <th colspan="3">Acciones</th>
      </tr>
      </thead>

      <tbody>
      <% @employees.each do |employee| %>
          <tr>
            <td><%= employee.first_name %></td>
            <td><%= employee.last_name %></td>
            <td><%= employee.city %></td>
            <td><%= employee.neighborhood %></td>
            <td><%= employee.street_number %></td>
            <td><%= employee.email %></td>
            <td><%= employee.age %></td>
            <td><%= employee.celphone %></td>
            <td><%= employee.date_hire %></td>
            <td><%= link_to 'Show', employee, :class => 'btn btn-info' %></td>
            <td><%= link_to 'Edit', edit_employee_path(employee), :class => 'btn btn-warning' %></td>
            <td><%= link_to 'Destroy', employee, :class => 'btn btn-danger', method: :delete, data: { confirm: 'Estas seguro?' } %></td>
          </tr>
      <% end %>
      </tbody>
    </table>
    </div>
   </div>

    <%= link_to 'Nuevo Empleado', new_employee_path, :class => "btn btn-success" %>





























