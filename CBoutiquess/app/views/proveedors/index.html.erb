
  <!-- Static navbar -->
  <nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand" href="<%= root_path %>">Control Boutique</a>
      </div>
      <div id="navbar" class="navbar-collapse collapse">
        <ul class="nav navbar-nav">
          <li ><a href="<%= user_path %>">Home</a></li>
          <li ><a href="<%= employees_path %>">Empleados</a></li>
          <li class="active"><a href="<%= proveedors_path %>">Proveedores</a></li>
          <li><a href="<%= products_path %>">Productos</a></li>
          <li><a href="#">Ventas</a></li>
          <li ><a href="<%= catalogs_path %>">Galeria</a></li>
          <li ><a href="<%= catalogo_usuarios_path %>">Catalogo de Usuarios</a></li>
          <li ><a href="<%= us_index_path %>">Nosotros</a></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <li ></li>
          <li><%= link_to "Cerrar sesion", logout_path, method: :delete %></li>
        </ul>
      </div><!--/.nav-collapse -->
    </div><!--/.container-fluid -->
  </nav>



<p id="notice"><%= notice %></p>

  <div class="jumbotron">
    <h2 class="sub-header">Lista de  proveedores</h2>
    <div class="table-responsive">
      <table class="table table-striped">
        <thead>
    <tr>
      <th>Name</th>
      <th>Phone</th>
      <th>Email</th>
      <th>City</th>
      <th>Neighborhood</th>
      <th>Street number</th>
      <th>RFC</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @proveedors.each do |proveedor| %>
      <tr>
        <td><%= proveedor.name %></td>
        <td><%= proveedor.phone %></td>
        <td><%= proveedor.email %></td>
        <td><%= proveedor.city %></td>
        <td><%= proveedor.neighborhood %></td>
        <td><%= proveedor.street_number %></td>
        <td><%= proveedor.pro_rfc %></td>
        <td><%= link_to 'Show', proveedor, :class => 'btn btn-info' %></td>
        <td><%= link_to 'Edit', edit_proveedor_path(proveedor), :class => 'btn btn-warning'  %></td>
        <td><%= link_to 'Destroy', proveedor, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-danger' %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>

<%= link_to 'New Proveedor', new_proveedor_path,  :class => "btn btn-success"  %>

