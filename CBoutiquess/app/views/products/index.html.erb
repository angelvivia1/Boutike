<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="<%= root_path %>">Control Boutique</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li ><a href="<%= user_path %>">Home</a></li>
        <li ><a href="<%= employees_path %>">Empleados</a></li>
        <li ><a href="<%= proveedors_path %>">Proveedores</a></li>
        <li class="active"><a href="<%= products_path %>">Productos</a></li>
        <li class=""><a href="<%=  %>">Ventas</a></li>
        <li ><a href="<%= catalogs_path %>">Galeria</a></li>
        <li ><a href="<%= catalogo_usuarios_path %>">Catalogo de Usuarios</a></li>
        <li ><a href="<%= us_index_path %>">Nosotros</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li ></li>
        <li><%= link_to "Cerrar sesion", logout_path, method: :delete %></li>
      </ul>
    </div><!--/.nav-collapse -->
  </div><!--/.container-fluid -->
</nav>


<p>
  Download:
    <span class="label label-default">
  <a href=" <%= products_path(format: "pdf") %>"> PDF </a> </span> |
   <span class="label label-success">
  <a href=" <%= products_path(format: "csv") %>"> EXCEL </a> </span>
</p>

<p id="notice"><%= notice %></p>
<div class="jumbotron">
<h2>Lista de productos</h2>
  <div class="table-responsive">
    <table class="table table-striped">
  <thead>

    <tr>
      <th>Codigo</th>
      <th>Marca</th>
      <th>Talla</th>
      <th>Tipo</th>
      <th>Descripción</th>
      <th>Proveedor</th>
      <th>Precio</th>
      <th>Imagen</th>
      <th>Status</th>
      <th>Existencias</th>
      <th colspan="3"></th>
    </tr>

  </thead>
  <tbody>
  <%  @products.each do |product| %>
      <tr>
        <td><%= product.code_product %></td>
        <td><%= product.brand_product %></td>
        <td><%= product.size_product %></td>
        <td><%= product.type_product %></td>
        <td><%= product.description_product %></td>
        <td><%= product.supplier_product %></td>
        <td><%= number_to_currency(product.price_product) %></td>
        <td><%= image_tag(product.image_product, :style => 'width:100px') %></td>
        <td><%= product.status_product %></td>
        <td><%= product.stocks %></td>
        <td><%= link_to 'Show', product, :class => 'btn btn-info' %></td>
        <td><%= link_to 'Edit', edit_product_path(product), :class => 'btn btn-warning' %></td>
        <td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' }, :class => 'btn btn-danger' %></td>
      </tr>
  <% end %>
  </tbody>
    </table>
  </div>
</div>

<%= link_to 'New Product', new_product_path,  :class => "btn btn-success" %>

